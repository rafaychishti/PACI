import { PagePlaceholder } from '@/shared/components/page-placeholder';

export function StudyLogPage() {
  return (
    <PagePlaceholder
      title="Study Log"
      description="Logging and browsing study sessions — date, duration, phase/module, and notes."
      comingIn="Sprint 2 — logging form and session list."
    />
  );
}
