import { PagePlaceholder } from '@/shared/components/page-placeholder';

export function CurriculumPage() {
  return (
    <PagePlaceholder
      title="Curriculum"
      description="The 9 PACI phases and their modules, seeded from the repository roadmap."
      comingIn="Sprint 2 — read-only phase/module browsing."
    />
  );
}
