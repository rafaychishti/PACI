import { PagePlaceholder } from '@/shared/components/page-placeholder';

export function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Portfolio projects, tracked from planned through shipped and deployed."
      comingIn="Sprint 3 — project CRUD."
    />
  );
}
