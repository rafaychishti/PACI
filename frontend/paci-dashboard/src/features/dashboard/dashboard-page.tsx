import { PagePlaceholder } from '@/shared/components/page-placeholder';

export function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="The home view — current phase, schedule variance, streak, and recent activity."
      comingIn="Sprint 3 (basic dashboard) and Sprint 4 (Engineering Score, milestones)."
    />
  );
}
